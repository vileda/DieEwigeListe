== Die Ewige Liste

With this litte rails app it is possible to save and sum up matches of games, console etc. where two players play against each other.
We use this to count xbox fifa matches.

There is an simple basic auth login, you need to setup the db with a username and a secret. look at application.rb


This is free software, do what you want with it, no warranties.